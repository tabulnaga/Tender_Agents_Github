[project]
name = "tender-agents-github"
version = "0.1.0"
description = "CrewAI HITL test project"
requires-python = ">=3.10"

dependencies = [
    "crewai",
    "requests"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"